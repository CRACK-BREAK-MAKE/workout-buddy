# Force pnpm to use standard fetch logic
registry=https://registry.npmjs.org/
network-concurrency=1
strict-peer-dependencies=false
auto-install-peers=true
# Bypass the internal fetch bug by using a traditional linker
node-linker=hoisted
